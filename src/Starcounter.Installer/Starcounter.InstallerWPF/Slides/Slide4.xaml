<Grid x:Class="Starcounter.InstallerWPF.Slides.Slide4"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                >


    <Grid >

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
    <StackPanel >

        <!--<Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Installing..." Style="{DynamicResource SlideHeader}" />
            <TextBlock Grid.Column="1" Text="Slide 4 of 5" Style="{DynamicResource SlidePage}" />
        </Grid>-->


        <StackPanel Margin="0,30,0,0"  >
            <TextBlock TextAlignment="Center" TextWrapping="Wrap" Text="Check out the slide show while waiting" Style="{DynamicResource SlideLevel1}"/>
            <TextBlock TextAlignment="Center" TextWrapping="Wrap" Text="(hit the back button a few times)" Style="{DynamicResource SlideLevel2}"/>

        </StackPanel>

    </StackPanel>


        <TextBlock Grid.Row="1" Text="Slide 4 of 4" Style="{DynamicResource SlidePage}" HorizontalAlignment="Right" />

    </Grid>

</Grid>
