<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <CoreCompileDependsOn>$(CoreCompileDependsOn);GeneratePuppetCode;GenerateTypedJsonCode</CoreCompileDependsOn>
  </PropertyGroup>
  <ItemGroup>
    <AvailableItemName Include="Puppet"/>
    <AvailableItemName Include="Typed_JSON"/>
  </ItemGroup>
     
  <!-- TODO: Temporary path. Use installation directory for starcounter later when Apps is integrated into main Starcounter -->
 <UsingTask TaskName="Starcounter.Internal.MsBuild.JsonToPuppetCsMsBuildTaskNoLocking" AssemblyFile="..\..\bin\$(Configuration)\Starcounter.MsBuild.dll" />


  <Target Name="GeneratePuppetCode" Inputs="@(Puppet)" Outputs="@(Puppet->'$(IntermediateOutputPath)%(FileName)%(Extension).g.cs')">
    <JsonToPuppetCsMsBuildTaskNoLocking
       InputFiles="@(Puppet)"
       OutputFiles="@(Puppet->'$(IntermediateOutputPath)%(FileName)%(Extension).g.cs')">
       <Output TaskParameter="OutputFiles" ItemName="puppetFiles" /> 
    </JsonToPuppetCsMsBuildTaskNoLocking>
	<ItemGroup>
      <Compile Include="@(puppetFiles)" />
    </ItemGroup>
  </Target>

 <UsingTask TaskName="Starcounter.Internal.MsBuild.JsonToTypedJsonCsMsBuildTaskNoLocking" AssemblyFile="..\..\bin\$(Configuration)\Starcounter.MsBuild.dll" />

  <Target Name="GenerateTypedJsonCode" Inputs="@(Typed_JSON)" Outputs="@(Typed_JSON->'$(IntermediateOutputPath)%(FileName)%(Extension).g.cs')">
    <JsonToTypedJsonCsMsBuildTaskNoLocking
       InputFiles="@(Typed_JSON)"
       OutputFiles="@(Typed_JSON->'$(IntermediateOutputPath)%(FileName)%(Extension).g.cs')">
       <Output TaskParameter="OutputFiles" ItemName="Typed_JSONFiles" /> 
    </JsonToTypedJsonCsMsBuildTaskNoLocking>
	<ItemGroup>
      <Compile Include="@(Typed_JSONFiles)" />
    </ItemGroup>
  </Target>
  
</Project>


