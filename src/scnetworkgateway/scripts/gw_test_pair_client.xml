<?xml version="1.0" encoding="utf-8"?>
<NetworkGateway>
  <!-- IP address of the master node -->
  <MasterIP>127.0.0.1</MasterIP>

  <!--
  List of local interfaces to bind to.
  Declare them when you expect the number of outgoing
  connections(e.g. due to proxying) to be more than 64K.
  -->
  <!--
  <BindingIP>10.0.0.1</BindingIP>
  <BindingIP>10.0.0.2</BindingIP>
  <BindingIP>10.0.0.3</BindingIP>
  <BindingIP>10.0.0.4</BindingIP>
  <BindingIP>10.0.0.5</BindingIP>
  -->

  <!-- Standard services ports -->
  <HttpPort>0</HttpPort>
  <HttpsPort>0</HttpsPort>
  <WebSocketsPort>0</WebSocketsPort>
  <GenSocketsPort>0</GenSocketsPort>
  <AggregationPort>0</AggregationPort>
  
  <!-- Number of worker threads -->
  <WorkersNumber>1</WorkersNumber>
  
  <!-- Maximum number of connections -->
  <MaxConnections>1000000</MaxConnections>

  <!-- Inactive session life time in seconds -->
  <InactiveSessionTimeout>1200</InactiveSessionTimeout>

  <!-- Gateway global statistics port -->
  <GatewayStatisticsPort>12345</GatewayStatisticsPort>

  <!-- Information on reverse proxies if any. -->
  <!--
  <ReverseProxies>
      
    <ReverseProxy>
      <ServerIP>69.16.244.74</ServerIP>
      <ServerPort>80</ServerPort>
      <GatewayProxyPort>80</GatewayProxyPort>
      <ServiceUri>/forum</ServiceUri>
    </ReverseProxy>

    <ReverseProxy>
      <ServerIP>69.16.244.74</ServerIP>
      <ServerPort>80</ServerPort>
      <GatewayProxyPort>80</GatewayProxyPort>
      <ServiceUri>/wiki</ServiceUri>
    </ReverseProxy>
    
    <ReverseProxy>
      <ServerIP>69.16.244.74</ServerIP>
      <ServerPort>80</ServerPort>
      <GatewayProxyPort>80</GatewayProxyPort>
      <ServiceUri>/queue-signup.php</ServiceUri>
    </ReverseProxy>
      
  </ReverseProxies>
  -->

  <!-- In which test mode Gateway operates. -->
  <TestingMode>MODE_GATEWAY_RAW</TestingMode>

  <!-- Server test port. -->
  <ServerTestPort>123</ServerTestPort>

  <!-- Number of connections to make to master node from this client -->
  <NumConnectionsToMaster>100</NumConnectionsToMaster>

  <!-- Number of tracked echoes to master node from this client -->
  <NumEchoesToMaster>5000000</NumEchoesToMaster>

  <!-- Maximum test time in seconds. -->
  <MaxTestTimeSeconds>1000</MaxTestTimeSeconds>

  <!-- Gateway-only HTTP requests per second. -->
  <ReportStatisticsName>Workers4Conns1000GwHttpRequestsPerSec</ReportStatisticsName>

</NetworkGateway>
